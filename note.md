# 模块类 
Character 包含各种用于处理 char 的有用函数，包括 Character.isLetterOrDigit() 和 Character.isDigit()
# 查询长度的方法区别
对数组用.length，因为数组是个基本类型，属性直接获取就行
对String用.length(),因为String是个引用类型，是个对象，对于对象想要获取某个属性需要用方法。
# 断言
assert 条件 : "错误提示";
默认关闭，java -ea Main开启。-ea = enable assertions。
